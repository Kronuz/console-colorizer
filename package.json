{
  "name": "console-colorizer",
  "version": "0.1.3",
  "description": "Adds colored console logging (for node and browser)",
  "main": "dist/index",
  "typings": "dist/index",
  "scripts": {
    "build": "tsc src/index.ts --outDir dist -d --lib dom,es5,es2015,scripthost",
    "prepublish": "npm run build"
  },
  "author": "German Mendez Bravo (Kronuz)",
  "license": "MIT",
  "dependencies": {
    "color-name": "^1.1.3"
  },
  "devDependencies": {
    "typescript": "^2.9.1"
  }
}
